[build]
builder = "nixpacks"
buildCommand = "cd agent && npm install && npm run build && cd ../x402-server && npm install && npm run build && cd ../mcp-server && npm install && npm run build"

[deploy]
startCommand = "node agent/dist/index.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5
