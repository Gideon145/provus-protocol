[build]
builder = "nixpacks"
buildCommand = "npm install && npx tsc -p agent/tsconfig.json"

[deploy]
startCommand = "node agent/dist/index.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5
