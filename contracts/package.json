{
  "name": "provus-contracts",
  "version": "0.1.0",
  "description": "PROVUS — Solidity contracts for 0G Chain",
  "private": true,
  "type": "module",
  "scripts": {
    "compile": "hardhat compile",
    "deploy:testnet": "node --import tsx/esm scripts/deploy.ts",
    "deploy:mainnet": "node --import tsx/esm scripts/deploy.ts",
    "deploy:archive": "node --import tsx/esm scripts/deployArchive.ts",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^4.0.7",
    "@nomicfoundation/hardhat-toolbox": "^6.1.2",
    "@openzeppelin/contracts": "^5.3.0",
    "dotenv": "^16.4.5",
    "ethers": "^6.11.1",
    "hardhat": "^3.0.0",
    "ts-node": "^10.9.2",
    "tsx": "^4.19.2",
    "typescript": "^5.4.5"
  }
}
